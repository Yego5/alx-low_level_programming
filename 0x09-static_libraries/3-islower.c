#include "main.h"

/**
 * _islower - checks for lowercase character
 * @k: the character to check
 * Return: 1 if k is lowercase, 0 instead
 */
int _islower(int k)
{
	return (k >= 'a' && k <= 'z');
}
